const React = require('react');
const Default = require('./layouts/default');

const Index = () => {
    return (
        <Default>
            <h2>Indx Page</h2>
        </Default>
    );
};

module.exports = Index;

